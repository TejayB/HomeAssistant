mqtt:
  discovery: true
  discovery_prefix: homeassistant
  broker: 10.0.0.26
  username: !secret mqtt_username
  password: !secret mqtt_password
  birth_message:
      topic: 'hass/status'
      payload: 'online'
  will_message:
      topic: 'hass/status'
      payload: 'offline'
